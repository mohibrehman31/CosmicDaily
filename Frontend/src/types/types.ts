export interface SolData {
  First_UTC: string;
  AT: {
    av: number;
    mn: number;
    mx: number;
  };
  WD: {
    // Add properties for wind direction if needed
  };
  // Add other properties as needed
}